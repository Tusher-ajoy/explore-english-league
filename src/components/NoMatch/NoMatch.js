import React from 'react';

const NoMatch = () => {
    return (
        <div style={{display:'flex', alignItems:'center', justifyContent:'center', color:'red', height:'100vh'}}>
            <h1>404!! Router not found</h1>
        </div>
    );
};

export default NoMatch;